@model CourseProject.Models.Service

@{
    ViewData["Title"] = "Delete";
}

<h1 class="title titleFont text-danger">Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div class="content-container">
    <h4 class="header headerFont">Service</h4>
    <hr class="mb-4" />
    <dl class="row">
        <dt class="col-sm-2 text-dark">
            @Html.DisplayNameFor(model => model.Type)
        </dt>
        <dd class="col-sm-10 text-dark">
            @Html.DisplayFor(model => model.Type)
        </dd>
        <dt class="col-sm-2 text-dark">
            @Html.DisplayNameFor(model => model.Rate)
        </dt>
        <dd class="col-sm-10 text-dark">
            @Html.DisplayFor(model => model.Rate)
        </dd>
        <dt class="col-sm-2 text-dark">
            @Html.DisplayNameFor(model => model.Requirements)
        </dt>
        <dd class="col-sm-10 text-dark">
            @Html.DisplayFor(model => model.Requirements)
        </dd>
    </dl>

    <form asp-action="Delete" class="form-container">
        <input type="hidden" asp-for="ServiceID" />
        <div>
        <input type="submit" value="Delete" class="btn btn-danger btn-lg rounded" /> 

        </div>
        <a asp-action="Index" class="btn btn-link text-secondary">Back to List</a>
    </form>
</div>
